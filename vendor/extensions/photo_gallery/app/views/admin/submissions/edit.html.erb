

<h1>Editing Photo</h1>
<%= error_messages_for :submission %>

<% form_for(:submission, :url => object_url, :html => { :method => :put, :multipart => true }) do |f| -%>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag t("update") %> or <%= link_to t("cancel"), collection_url %>
  </p>
<% end %>